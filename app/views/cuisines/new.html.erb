<h1>Cadastre sua nova cozinha no sistema:</h1>

<% if @cuisine.errors.any? %>
  <p>Não foi possível salvar o tipo da receita.</p>
   <ul>
    <% @cuisine.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
<% end %>

<%= form_with model: @cuisine, local: true do |f| %>
  <%= f.label :name, 'Nome' %>
  <%= f.text_field :name %>
  <%= f.submit 'Enviar' %>
<% end %>