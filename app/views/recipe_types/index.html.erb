<h1>Receitas cadastradas no sistema:</h1>
<ul>
  <% @recipe_types.each do |recipe_type| %>
    <li><%= link_to recipe_type.name, recipe_type %></li>
  <% end %>
</ul>

<%= link_to 'Voltar', root_path, class: 'btn btn-secondary' %>