<h1>Cadastre seu novo tipo de receita abaixo</h1>
<%= form_with model: @recipe_type, local: true do |f| %>
  <%= f.label :name, 'Nome' %>
  <%= f.text_field :name %>
  <%= f.submit 'Enviar' %>
<% end %>